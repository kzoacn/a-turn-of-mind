**第一阶段交付记录 · 2026-09-22**

已完成可试玩原型：八个关卡、共用插槽的两个转环、正逆操作、沿轨道移动的动画、范围预览、目标叠看、无限撤销与重做、重来、自由选关、分级提示、完成反馈、基础音效、动画设置、可选原理札记、自动存档及本地试玩记录导出。

程序和画面来自同一个 Godot 4.7.2 项目，可导出 Windows 与 Web。Windows 启动入口是项目根目录的 `StartGame.bat`；分发文件在 `build/BorrowedMoves-windows.zip`。浏览器启动方式见 README。

八关的最短解长度分别为 1、1、2、3、4、6、6、8。Python 离线 BFS 与游戏使用的 GDScript 求解器交叉核对，具体证书保存在 `level-validation.json`。不同解法同样可以通关，主线没有步数限制。

验证内容：

最终自动检查结果：265 项逻辑断言、38 项引擎交互断言全部通过；最终 Web 分发版本八关验收通过，浏览器及游戏控制台无错误。包的大小与 SHA-256 记录在 [validation.json](validation.json)，实机画面见 [preview.png](preview.png)。

- 逻辑测试覆盖逆操作、四次转动恢复、未受影响插槽、多重符号、可达与不可达状态、有界搜索、八条解法、撤销/重做、存档重放、损坏存档恢复。
- 引擎交互测试通过真实输入事件操作按钮与中央把手，覆盖八关通关、键盘操作、动画期间输入、切关、分级提示、帮助窗口、保存对话框与重新打开游戏。
- Chromium 对导出的 Web 版本执行八关鼠标通关、键盘撤销/重做、提示、JSON 记录下载、刷新后进度恢复和帮助窗口；查看了 1280×800 与 960×600 截图。
- Windows 导出的 EXE 已分别执行无窗口启动检查和实际图形窗口启动检查，均正常退出。桌面版本的全部操作仍建议在试玩者自己的 Windows 设备上体验。
- 两个分发 ZIP 的 CRC 校验通过，并附有 Godot 与字体的第三方许可。

引擎测试日志位于 `.tools/build.log`，浏览器测试输出和截图位于 `test-results/`。这些都是自动化检查，不能计为真人试玩反馈。

**下一步是第一轮真人试玩。** 使用 `PLAYTEST.md`，先找 3–5 人排除明显困惑，再换人测试修改。尤其观察第 3 关是否读懂共用插槽、第 5–6 关是否发现可迁移的技巧、第 7 关是否理解相同徽章可以替代。

目前尚未验证 15–20 分钟的目标体验时长，也没有验证满意度或继续游玩的意愿。宏操作、完整商业美术、Steam 集成与正式关卡体量仍保留到原型反馈之后。
